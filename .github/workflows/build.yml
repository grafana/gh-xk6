name: build
on:
  push:

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install devcontainer CLI
        run: npm install -g @devcontainers/cli@0

      - name: Run devcontainer build
        run: |
          devcontainer build --workspace-folder ${{github.workspace}} --cache-from ghcr.io/grafana/gh-xk6-devcontainer:latest

#      - name: Run in dev container
#        uses: devcontainers/ci@v0.3
#        with:
#          imageName: ghcr.io/grafana/gh-xk6-devcontainer
#          runCmd: |
#            goreleaser build --clean --snapshot --single-target
