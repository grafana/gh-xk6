name: Publish TechDocs
on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'catalog-info.yaml'
      - '.github/workflows/publish-techdocs.yaml'
concurrency:
  group: '${{ github.workflow }}-${{ github.ref }}'
  cancel-in-progress: true
permissions: {}
jobs:
  publish-docs:
    uses: grafana/shared-workflows/.github/workflows/publish-techdocs.yaml@84e8abf0d3cd31cc8fa01d0e2c629a96864a108a # main
    permissions:
      contents: read
      id-token: write
    with:
      namespace: default
      kind: component
      name: gh-xk6
      default-working-directory: .
